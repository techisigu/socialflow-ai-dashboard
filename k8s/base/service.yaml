apiVersion: v1
kind: Service
metadata:
  name: socialflow-backend
  labels:
    app: socialflow-backend
spec:
  selector:
    app: socialflow-backend
  ports:
    - name: http
      port: 80
      targetPort: http
